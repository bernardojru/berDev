import { styled } from "../.";

export const CVContainer = styled("div", {
  background: "$black",
  color: "$gray100",
  display: "flex",
  flexDirection: "column",
  fontFamily: "$default",
});
export const CVConst = styled("div", {
  marginTop: "$1",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});
